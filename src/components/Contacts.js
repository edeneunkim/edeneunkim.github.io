import * as React from 'react';

function Contacts() {
    return (
        <div id='contacts' className="h-screen flex items-center justify-center">
            <p className="text-primary">Contacts</p>
        </div>
    );
}

export default Contacts;