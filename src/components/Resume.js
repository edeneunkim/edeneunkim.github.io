import * as React from 'react';

function Resume() {
    return (
        <div id='resume' className="h-screen flex items-center justify-center">
            <p className="text-primary">Resume</p>
        </div>
    );
}

export default Resume;